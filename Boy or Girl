#include <bits/stdc++.h>
using namespace std;
int main(){
string username;
cin>>username;
set<int>s;
for(int i=0;i<username.length();i++){
    s.insert(username[i]);
}
int a=s.size();
if(a%2==0){
    cout<<"CHAT WITH HER!";
}
else{
    cout<<"IGNORE HIM!";
}
    return 0;
}
